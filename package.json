{
  "name": "meteor-1.8-coffeescript-example",
  "version": 1.0.0,
  "author": "Nicholas Sardo <gcc.programmer@gmail.com> (http://nicksardo.wordpress.com)",
  "description": "Basic Meteor 1.8 CoffeeScript Project including Meteor Methods and Jade",
  "keywords": ["CoffeeScript", "Meteor"],
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha",
    "test-app": "TEST_WATCH=1 meteor test --full-app --driver-package meteortesting:mocha",
    "visualize": "meteor --production --extra-packages bundle-visualizer"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0",
    "meteor-node-stubs": "^0.4.1"
  }
}
